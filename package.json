{
  "name": "cryptokhabar",
  "version": "1.0.0",
  "description": "India's Blockchain Token News Portal",
  "scripts": {
    "fetch-data": "node scripts/fetch-data.js",
    "build": "npm run fetch-data && npm run copy-files",
    "copy-files": "node scripts/copy-files.js"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "fs-extra": "^11.2.0",
    "node-cron": "^3.0.3"
  }
}